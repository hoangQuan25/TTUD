#include <bits/stdc++.h>
using namespace std;

// deque: element can be pushed/ popped from both of ends
// methods:
// size()
// push_front() and push_back()
// pop_front() and pop_back()
// empty() ? -> bool
// deque.front() -> front element
// deque.back() -> last element

// exercises in deque: sliding window maximum
// given an array, for each subarray with n elements print out the max value in it
// eg: 2 3 7 9 with window 3: => 7 9
int main() {
	
}
